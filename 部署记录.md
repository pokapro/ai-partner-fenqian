# 部署记录

## 最终部署信息

| 项目 | 内容 |
|------|------|
| 公网 URL | https://ai-partner-fenqian.onrender.com |
| 部署平台 | Render.com (Free 计划) |
| AI Provider | DeepSeek API |
| GitHub 仓库 | pokapro/ai-partner-fenqian |

## 注意
- Render免费版15分钟无请求会休眠，下次请求会冷启动（约30秒）
- SQLite 数据在 Render 重启会丢失（免费版无持久磁盘）
- 需要设置环境变量：AI_PROVIDER=deepseek, DEEPSEEK_API_KEY=..., ADMIN_TOKEN=dev-token-2026

## 验证结果
- 首页 ✅
- /api/health ✅ (provider: deepseek)
- 表单提交+AI生成 ✅ (2020字符，含历史参考模块)
- 0元出资 ✅
- 历史案例参考 ✅

## 环境变量设置方式
用 Render API 直接 PUT：
```bash
curl -X PUT "https://api.render.com/v1/services/{serviceId}/env-vars" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $RENDER_API_KEY" \
  -d '[
    {"key":"AI_PROVIDER","value":"deepseek"},
    {"key":"DEEPSEEK_API_KEY","value":"***"},
    {"key":"DEEPSEEK_MODEL","value":"deepseek-chat"},
    {"key":"ADMIN_TOKEN","value":"***"}
  ]'
```

⚠️ 注意：body 是数组 `[]` 格式，不是 `{"envVars":[]}`

## 触发部署
```bash
curl -X POST "https://api.render.com/v1/services/{serviceId}/deploys" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $RENDER_API_KEY" \
  -d '{"serviceId":"..."}'
```

## 踩坑记录
1. Render UI 添加环境变量操作不稳定，AppleScript keystroke 在 Chrome 后台时常失效
2. Secret File (/.env) 挂载路径是 /etc/secrets/，但代码 dotenv 默认读不到
3. Render API 的 env-vars PUT 必须用 `[]` 数组格式
4. DEEPSEEK_API_KEY 设置时被 shell 变量截断导致 401 错误，需确认完整 key
5. 环境变量设置后必须重新触发部署才能生效

## 2026-06-09 13:41 (V0.4 补 — 部署修复三连)

### 问题链

| 步骤 | Commit | 问题 | 状态 |
|------|--------|------|------|
| 1 | 1303ba4 | UI设计改版（21处反模式替换） | ❌ 部署失败 |
| 2 | 70128c7 | suggest-form JSON解析修复 | ❌ 叠加失败 |
| 3 | 193c928 | **根因修复**：copilotkit.js函数名重构(createCopilotKitHandler)但index.js仍引用setupCopilotKit | ✅ 部署成功但CopilotKit路由仍404 |
| 4 | 0c0df70 | **CopilotKit路由404**：single-route模式下`app.use("/api/copilotkit", handler)`会剥离前缀，改为`app.use(handler)` | ✅ 路由通但agent找不到 |
| 5 | 5d48cda | **Agent not found**：CopilotRuntime用`remoteEndpoints`旧格式在v1.59.5不兼容，改为`agents: { fenqian: agent }` | ✅ 路由+agent正常 |
| 6 | cacbb4e | **侧边栏消息消失**：React.StrictMode导致重渲染清除状态 | ✅ 已部署 |

### 根因总结
1. **函数名重构未同步 import**：`setupCopilotKit → createCopilotKitHandler` 后 index.js 未更新 require → `is not a function`
2. **CopilotKit Express挂载方式错误**：`single-route` mode下 handler内部basePath已含完整路径，外部`app.use`不能再带路径参数（会剥离前缀导致handler内部的POST route匹配不上）
3. **CopilotRuntime agent注册格式错误**：v1.59.5用了`remoteEndpoints: [{url, agent}]`旧格式，应改为`agents: { fenqian: agent }`
4. **React.StrictMode**：导致CopilotKit的SSE连接在重渲染时重置，消息显示后消失

### 验证结果
- ✅ `/api/health` → provider: deepseek, version: 0.3.0
- ✅ `/api/copilotkit` POST → 200 (streaming SSE)
- ✅ 前端页面正常加载
- ✅ CopilotSidebar打开后消息不消失
