# 进度日志

## 2026-06-05 (Day 1)

### 完成状况：✅ 完成

### 今天完成的工作

#### 项目骨架搭建
- [x] 创建项目结构（package.json、README.md、.env.example、.env）
- [x] 创建目录：data/exports、public、server、docs

#### 后端
- [x] `server/index.js` - Express 主入口，含全部 API 路由
- [x] `server/db.js` - SQLite 数据库（sql.js，纯 JS 无原生编译依赖）
- [x] `server/ai.js` - AI Provider 抽象（支持 ollama/GLM/通义千问）
- [x] `server/prompt.js` - 系统 Prompt + 用户 Prompt 模板
- [x] `server/report.js` - 利润模拟表计算（程序化，非 AI 生成）

#### 前端
- [x] `public/index.html` - 移动端适配的落地页
- [x] `public/styles.css` - 样式（基于 Tailwind CDN）
- [x] `public/app.js` - 表单交互、校验、渲染、付款意向记录

#### 文档
- [x] `README.md` - 项目说明
- [x] `docs/test-cases.md` - 3 个测试案例
- [x] `docs/review-checklist.md` - 人工审核清单

#### 数据库
- [x] SQLite `cases` 表 | 字段：id, created_at, source, contact, partner_count, input_json, report_markdown, payment_intent, review_status, review_note

### 验证结果
- ✅ 服务启动正常 - `http://localhost:3000` 可访问
- ✅ API `/api/health` 返回正常
- ✅ 前端首页表单字段可见
- ✅ 表单校验正常运行（缺联系方式返回错误、纠纷关键词检测返回提示、人数校验通过）
- ✅ 数据库初始化正常

### 技术决策
1. 因 Node 26 的 C++20 要求，`better-sqlite3` 编译失败，改用 `sql.js`（纯 JS SQLite 实现，无需原生编译）
2. AI 默认使用 Ollama 本地（qwen2.5:7b），降低开发成本

## 2026-06-05 (Day 1 - 后续修复)

### 完成状况：✅ 完成

### 根据外部评审意见修复的问题

#### P0（优先级最高）
- [x] **保护 /api/cases 和 /api/cases/:id**：加了 ADMIN_TOKEN 中间件，仅本地或带 token 可访问
- [x] **允许 0 元出资**：后端 validateInput 允许 capital=0（要求 effortType + responsibility 必填），前端同样放开校验

#### P1
- [x] **增加审核接口 PUT /api/cases/:id/review**：需 ADMIN_TOKEN，支持 reviewed/delivered/rejected 状态
- [x] **修测试案例 3**：effortType 从 `兼职供应链` 改为 `兼职`，与下拉选项一致；docs/test-cases.md 同步更新
- [x] **改 Prompt 模块数量 10 → 8**：server/prompt.js 系统指令和用户提示均已修正

#### P2
- [x] **前端用 marked CDN**：public/index.html 添加 marked.min.js；app.js 改用 `marked.parse(text)`，保留 fallback
- [x] **.env + .env.example 加 ADMIN_TOKEN**：当前设为 `dev-token-2026`

### 影响范围
| 文件 | 改动说明 |
|------|---------|
| server/index.js | 加 requireAdminToken 中间件、review 接口、0元出资校验 |
| server/db.js | 已有 updateReviewStatus，无需改 |
| server/prompt.js | 模块数 10→8 |
| public/app.js | 0元出资校验、fillTestCase 案例3修正、marked 渲染 |
| public/index.html | 追加 marked CDN |
| docs/test-cases.md | 案例3 effortType 修正 |
| .env / .env.example | 加 ADMIN_TOKEN |

## 2026-06-05 (Day 1 - 数据库历史案例参考能力)

### 完成状况：✅ 完成

### 改动目标
AI 不再只根据表单内容生成报告，而是：
1. 从数据库读取历史相似案例
2. 结合当前案例 + 历史案例数据给出更准确的分配建议
3. 保证输出质量，不会跑偏

### server/db.js
- [x] 新增 `findSimilarCases(partners, limit=5, excludeId)` 方法
  - 匹配逻辑：根据出资模式（等额/不等额/0元出资）和出力类型组合进行相似度评分
  - 评分维度：合伙人数(+30)、出资模式(+30)、出力类型精确匹配(+20)、部分重叠(+10)、出资金额量级(+20)
  - 按相似度 + 时间排序，返回脱敏案例列表
- [x] 新增 `getCaseStats()` 方法
  - 返回：总案例数、有报告数、各分配方案采纳率、审核状态分布、付款意向比例、出资模式分布
- [x] 脱敏处理：只保留 partnerCount, fundingMode, effortTypes, totalCapital, allocationScheme
- [x] 新增辅助函数：`classifyCase()` 案例分类、`similarityScore()` 相似度评分、`sanitizeCase()` 脱敏、`extractAllocationScheme()` 提取分配方案

### server/prompt.js
- [x] 系统 Prompt 增加「历史案例参考」指引
- [x] AI 需要知道哪些方案在类似案例中更常用，但必须声明"历史数据仅供参考"
- [x] 新增 `buildReferenceContext(similarCases, stats)` 函数
  - 格式化输出平台统计 + 相似案例列表
  - 带 ⚠️ 警示声明
- [x] 导出新函数供 ai.js 使用

### server/ai.js
- [x] `generateReport` 函数增加 `dbRef` 参数
- [x] 调用 AI 前先查数据库获取相似案例 + 统计
- [x] 相似案例数据作为 system message 传给 AI
- [x] 所有 real provider（ollama/glm/deepseek/qwen）统一通过 `buildMessages()` 构建包含参考上下文的 messages
- [x] mock provider 也支持 referenceContext 参数
- [x] 数据库查询失败不影响主流程（非致命错误）

### server/index.js
- [x] `generateReport(req.body)` → `generateReport(req.body, db)` 传递数据库引用

### 验证结果
- ✅ node -c 语法检查全部通过
- ✅ 服务启动正常，DeepSeek API 调用成功
- ✅ 首次提交：AI 正常生成完整报告（DeepSeek 实际调用）
- ✅ 第二次提交（相似案例）：AI 报告中出现了参考数据（✅ Reference context was included by AI）
- ✅ `findSimilarCases` 正确匹配：不等额出资的案例排序靠前，0元出资案例排最后
- ✅ `getCaseStats` 返回正确统计：4个案例，平衡型2次、激励型1次、保守型1次
- ✅ 脱敏验证通过：无联系方式泄露
- ✅ mock provider 也包含了参考数据摘要（📊 参考数据摘要）

### 注意事项
- 数据库文件在测试过程中被清空重建（`rm -f data/app.db`），部署到 Render 前需要恢复线上数据
- 新功能需要在 Render 上重新部署后生效

## 2026-06-05 - 上线完成

### 🎉 Rendr 上线成功
- URL: https://ai-partner-fenqian.onrender.com
- AI Provider: DeepSeek (deepseek-chat)
- 数据库: SQLite (Render 重启会丢数据，需要数据持久化时迁移)
- 环境变量: Render API 已配置 (AI_PROVIDER / DEEPSEEK_API_KEY / DEEPSEEK_MODEL / ADMIN_TOKEN)

### 关键改进
- AI 读取数据库历史案例生成报告 ✅
- 完整8模块输出 ✅
- 0元出资支持 ✅
- 公网不开放（需 ADMIN_TOKEN 看案例列表）

### ⚠️ 注意
- Render 免费版 15分钟无请求会休眠，冷启动约30秒
- SQLite 数据非持久化（重启丢失）
- 后续可以迁移到阿里云香港 VPS（¥68/年）

## 2026-06-05 (P0/P1 修复)

### 完成状况：✅ 完成

### 根据评审意见修复的问题

#### P0
- [x] **/api/generate 不返回完整 reportMarkdown**：公开接口只返回 previewMarkdown、caseId、status，完整报告只能通过后台 api/cases/:id 查看
- [x] **.env 固定路径加载**：用 `path.join(__dirname, '..', '.env')` 替代依赖 cwd 的 dotenv.config()

#### P1
- [x] **后台接口支持 Authorization: Bearer header**：同时支持 ?token=xxx 和 Authorization: Bearer xxx

### 验证结果（线上）
- ✅ /api/health → provider: deepseek
- ✅ 提交案例 → 返回 previewMarkdown，无 reportMarkdown 泄漏
- ✅ 无 token → 403
- ✅ Bearer 正确 token → 200
- ✅ 错误 token → 403
- ✅ 后台可查看完整报告（5302字符）

## 2026-06-05 (V0.2 升级 — 案例库+规则库驱动)

### 完成状况：✅ 已完成并上线

### 改动内容
- **server/db.js**: 新增 3 张表（knowledge_cases / rules / templates）+ 10 个 CRUD 方法
- **server/matcher.js** (新建): 场景分类 + 案例/规则/模板匹配 + 三段式上下文构建
- **server/prompt.js**: 系统提示增加「禁止泄露后台数据」规则
- **server/ai.js**: generateReport 调用 matcher 获取上下文
- **server/index.js**: 新增 10 个 admin 接口 + 启动时调用 seed
- **server/seed.js** (新建): 5 规则 + 3 案例 + 5 模板（幂等插入）
- **public/app.js**: 报告后添加「已参考系统内相似案例和规则库生成」

### 线上验证结果
- ✅ /api/health → provider: deepseek, version: 0.2.0
- ✅ 种子数据: 3 案例 + 5 规则 + 5 模板
- ✅ 无 token → 403 / Bearer 正确 → 200 / 错误 → 403
- ✅ 生成报告不泄漏完整 reportMarkdown
- ✅ 报告含知识库参考

### 已知信息
- 线上 ADMIN_TOKEN = afu_admin_2026（通过 Render API 设置）
- 本地 .env ADMIN_TOKEN = dev-token-2026（两套不同）

## 2026-06-05 17:11 (前端稳定性修复)

### 完成状况：✅ 已完成

### 修复内容
- **CDN 脚本移到 body 底部**：Tailwind + marked 从 `<head>` 移到 `</body>` 前，不再阻塞页面 DOM 渲染
- **app.js 加 init() 自检**：`getElementById('partnerForm')` 返回 null 时 100ms 后重试
- **按钮事件防重复绑定**：`_listenerAttached` 标记防止多次绑定

### 修复后页面加载顺序
1. HTML 骨架先渲染（按钮、表单可见）
2. Tailwind CDN 加载
3. marked CDN 加载  
4. app.js 加载 → 绑定事件（DOM 没就绪则 100ms 重试）

### 线上验证
- 部署自动触发 ✅
- index.html CDN 脚本位置已修正 ✅
- app.js 含 init() 函数 ✅

### 待处理
- [ ] 用户反馈网页无法互动的问题需用户确认是否修复

## 2026-06-08 (吴老师U盘股权资料注入)

### 完成状况：✅ 本地完成，等待 Render 自动部署

### 完成内容
1. **提取吴老师U盘资料**：
   - 广东启正股权调整方案（真实家族企业三层架构案例）
   - 云南XX茶业分红退出机制（已运营公司案例）
   - 创业公司动态股权分配（Vesting案例）
   
2. **注入种子数据**（server/seed.js）：
   - 知识案例：3 → 6 条（+3 吴老师U盘）
   - 规则：5 → 12 条（+7 吴老师U盘）
   - 模板：5 → 11 条（+6 吴老师U盘）

3. **新增规则覆盖**：
   - 家族企业股权传承税务优惠、防火墙架构风险隔离、分红上限约定
   - Vesting机制、一致行动协议、特殊性税务处理、分红权与决策权分离

4. **Git仓库优化**：
   - 添加 .gitignore 排除吴老师U盘原始文件
   - 仅保留结构化后的seed数据在代码中

### 线上状态
- Render 自动部署已触发，等待构建完成

## 2026-06-08 (V0.3 — CopilotKit 重构部署)

### 完成状况：✅ 已部署上线

### 背景
用户评估 [CopilotKit](https://github.com/CopilotKit/CopilotKit)（33.7K⭐，MIT）后认为对项目非常有价值，要求直接替换现有前端+后端Agent层。

### 完成内容

#### 1. 依赖安装
- React 19 + React DOM
- @copilotkit/react-core / @copilotkit/react-ui / @copilotkit/runtime
- @ai-sdk/openai（DeepSeek 适配）
- vite + @vitejs/plugin-react
- zod（工具参数校验）

#### 2. 前端重构（src/）
| 文件 | 内容 |
|------|------|
| `src/main.jsx` | React 入口 |
| `src/App.jsx` | CopilotKit Provider + CopilotSidebar |
| `src/ChatApp.jsx` | 占位界面（引导语+使用案例） |
| `src/index.css` | 基础样式 |
| `index.html` | Vite 入口（替换原 public/index.html 前端展示） |
| `vite.config.js` | Vite + proxy 到后端 |

#### 3. 后端 Agent（server/copilotkit.js）
- **架构**：CopilotRuntime + BuiltInAgent + DeepSeek（@ai-sdk/openai 适配）
- **路由模式**：`single-route`，`POST /api/copilotkit`
- **工具**：`getKnowledgeContext` — AI 主动调用查询知识库匹配案例/规则/模板
- **协议**：AG-UI Protocol

#### 4. 后端兼容改造（server/index.js）
- 新增 `static serve` 两个目录：`dist/` 优先，`public/` 回退
- 新增 SPA fallback 路由
- 新增 `setupCopilotKit(app, db)` 调用
- 版本号：0.2.0 → 0.3.0

#### 5. 构建与部署
- `start.sh`：先 `vite build`，再 `node server/index.js`
- `package.json` script 更新：start → ./start.sh，新增 dev:frontend/build
- Git 推送到 main，Render 自动部署

### 关键坑（已踩完并记录）
1. **BuiltInAgent + defineTool 在 `@copilotkit/runtime/v2` 子路径**，不在主入口
2. **`COPILOTKIT_TELEMETRY_DISABLED=true`** 必须设置，否则 telemetry 崩溃导致启动失败
3. **Express 子路由用 `.use(router)`**，不用 `.use("/api/...", handler)` — router 自带 basePath
4. **single-route 模式**用 `createCopilotExpressHandler({ runtime, basePath, mode: "single-route", cors: true })`
5. **AG-UI 协议格式**：`{ method: "agent/run", params: { agentId }, body: { messages, threadId, runId, tools, context } }`
6. 前端 @copilotkit/react-core 自动处理协议细节，后端只需要暴露 POST 端点

### 架构变迁
```
V0.2: index.html + app.js → Express → ai.js → DeepSeek → Markdown
                                ↕
                            matcher.js + SQLite

V0.3: React + CopilotKit → AG-UI → CopilotRuntime → BuiltInAgent → DeepSeek
                                     ↕
                               getKnowledgeContext (工具)
                                     ↕
                               matcher.js + SQLite（原有不动）
```

### 线上验证
- ✅ version: 0.3.0
- ✅ 前端 `dist/index.html` 正常返回
- ✅ CopilotKit endpoint OPTIONS 204 / POST 200
- ✅ AG-UI 协议返回 RUN_STARTED 事件
- ✅ 旧 API 兼容（`/api/generate` + admin 接口）
- ✅ 知识库数据完整（6 KC + 12 Rules + 11 Templates）

### 待处理
- [ ] 用户端手动测试聊天互动流程
- [ ] 确认 COPILOTKIT_TELEMETRY_DISABLED env var 在 Render 上生效（通过 Render API 已设置需等重部署）

## 2026-06-08 11:27 (CopilotKit 方向纠偏)

### 评审结论
CopilotKit 集成方向没错，但当前 V0.3 把"产品主流程"做丢了。

### 核心问题
1. **ChatApp.jsx** 只是占位页，没有表单、报告预览、付款转化入口
2. **CopilotSidebar** 承担了全部交互，产品变成了"问AI"而不是"完成合伙分钱诊断"
3. **知识库工具** 已接入但未绑定主业务漏斗
4. **旧 API**（/api/generate/付款意向/人工审核）还在，应继续作为主流程

### 方向纠正
**主界面 = 合伙分钱工作台**
**CopilotKit = 右侧 AI 顾问（辅助/解释）**

不替代产品界面，不替代主业务漏斗。

### 待执行
- [ ] 重做 ChatApp.jsx → 结构化工作台（表单+报告+付款）
- [ ] 保留 CopilotSidebar 但定位为"AI 顾问"
- [ ] 修正单位：万元→元
- [ ] 更新 README 反映 V0.3 真实结构
- [ ] 验收：用户不打开聊天也能完成提交看到报告
